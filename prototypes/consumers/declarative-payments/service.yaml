service_id: f-payments
owner: payments@example.com
functions:
  - name: charge
    handler: charge.charge
    api: { method: POST, path: /charge }
  - name: nightly
    handler: nightly.nightly
    memory: 512
    schedule: "rate(1 day)"
