"""{{ project_name }} - generated from the Paved CDK template (SDK variant).

You rarely touch this file. Declare your service in ``handlers/*.py`` using the
SDK decorators (``@function`` / ``@scheduled`` / ``@api``) and storage via
``data_api(...)``. ``synth()`` imports the handlers, turns the registrations into
governed resources (in-VPC KMS Lambdas, private API, EventBridge) and synthesizes.

The platform resolves VPC, subnets, KMS, the IAM permissions boundary, log
retention and governance tags from the target account - you never pass them.
"""

from paved_cdk.sdk import synth

synth(
    service="{{ project_slug }}",
    tags={
        "Owner": "{{ owner_email }}",
        "Team": "{{ team }}",
        "CostCenter": "{{ cost_center }}",
    },
)
